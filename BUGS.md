# Bug Registry

Cumulative fixes tracked in `data/bug-registry.json`. Target: **10,000** fixes with unique tests.

- **BUG-00001** (ui/drag) — Drag preview wrapped to opposite board edge when shape off-board → test `preview.no-wrap.off-board` in `scripts/playtest.js`
