services:
  blockblast:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - blockblast-data:/app/server/data

volumes:
  blockblast-data:
