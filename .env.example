# Copy to .env and edit before production deploy
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# Required — use a long random string (e.g. openssl rand -hex 32)
JWT_SECRET=change-me-to-a-long-random-secret

# Public URL (optional) — e.g. https://game.yourdomain.com
PUBLIC_URL=
